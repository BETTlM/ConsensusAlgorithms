import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Consensus Algorithms in Blockchain: A Comparative Explorer",
  description:
    "Compare Proof of Work, Proof of Stake, DPoS, BFT, and other consensus protocols across the blockchain trilemma, real-world deployments, smart contracts, layers, and compatibility.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
    >
      <body className="min-h-full bg-page text-ink antialiased">{children}</body>
    </html>
  );
}
